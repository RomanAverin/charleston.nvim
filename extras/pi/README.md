# Charleston theme for Pi

Install the theme globally:

```sh
mkdir -p ~/.pi/agent/themes
cp extras/pi/charleston.json ~/.pi/agent/themes/
```

Then select `charleston` in Pi via `/settings`, or add it to `~/.pi/agent/settings.json`:

```json
{
  "theme": "charleston"
}
```
